import type { Metadata } from 'next';
import Courses from '~/lib/pages/courses';
import SingleCourses from '~/lib/pages/single-course';

export const metadata: Metadata = {
  title: 'Single Course',
};

export default SingleCourses;
