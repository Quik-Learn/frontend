import type { Metadata } from 'next';
import Resources from '~/lib/pages/resources';

import Wards from '~/lib/pages/wards';

export const metadata: Metadata = {
  title: 'Resources',
};

export default Resources;
