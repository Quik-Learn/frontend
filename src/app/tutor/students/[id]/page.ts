import type { Metadata } from 'next';

import SingleStudent from '~/lib/pages/tutor-page/single-student';

export const metadata: Metadata = {
  title: 'Single Student',
};

export default SingleStudent;
