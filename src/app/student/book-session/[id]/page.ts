import type { Metadata } from 'next';

import SingleSession from '~/lib/pages/student-page/single-session';

export const metadata: Metadata = {
  title: 'Single Session',
};

export default SingleSession;
